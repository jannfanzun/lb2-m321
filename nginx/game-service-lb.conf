events {
  worker_connections 1024;
}

http {
  upstream game_service_backend {
    ip_hash;  # Sticky sessions for WebSocket connections
    server game-service-1:3002 max_fails=3 fail_timeout=30s;
    server game-service-2:3002 max_fails=3 fail_timeout=30s;
  }

  # Rate limiting
  limit_req_zone $binary_remote_addr zone=game_service_limit:10m rate=10r/s;

  server {
    listen 3002;
    server_name _;

    # Increase buffer sizes
    client_max_body_size 10m;
    proxy_buffer_size 128k;
    proxy_buffers 4 256k;
    proxy_busy_buffers_size 256k;

    # Health check endpoint
    location /health {
      access_log off;
      proxy_pass http://game_service_backend;
      proxy_connect_timeout 5s;
      proxy_read_timeout 5s;
    }

    # Main API routes
    location / {
      # Rate limiting
      limit_req zone=game_service_limit burst=20 nodelay;

      # Proxy settings
      proxy_pass http://game_service_backend;
      proxy_http_version 1.1;

      # Headers
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      # Connection settings
      proxy_connect_timeout 30s;
      proxy_send_timeout 30s;
      proxy_read_timeout 30s;

      # Keepalive
      proxy_set_header Connection "";
    }

    # Error pages
    error_page 502 503 504 /50x.html;
    location = /50x.html {
      return 503;
    }
  }
}
